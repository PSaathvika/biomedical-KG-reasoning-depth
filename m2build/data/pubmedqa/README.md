# PubMedQA

Expected local file after download:

```text
data/pubmedqa/pqa_labeled.json
```

The labeled PubMedQA configuration contains expert-annotated questions. The later pilot/full experiment should freeze the question IDs before comparing methods.
