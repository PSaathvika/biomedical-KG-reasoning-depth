# Knowledge graph input

`demo_kg.json` is only a tiny smoke-test graph so the Milestone 2 method loops can be tested without an external KG.

For the actual research experiment, replace this demo graph with the fixed biomedical KG/corpus selected by the team. Do not mix different KG sources between methods.
