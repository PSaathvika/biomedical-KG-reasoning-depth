# MedMCQA

Expected local file after download:

```text
data/medmcqa/dev.json
```

The dev split is the labeled split intended for evaluation/development. The later 20-question pilot should sample a fixed subset from this file and save the selected IDs so every method receives exactly the same questions.
